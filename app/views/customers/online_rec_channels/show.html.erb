<div class="online_rec_channels p-5 text-center">
  <p class="introduce_channel fw-bold w-50 mx-auto">一斉配信チャンネルの紹介サイトは<a href="https://channel.everyplus.jp/" target="_blank">こちら</a></p>
  <h4 class="main_header mt-5 fw-bold pb-2"><%= @online_rec_channel.period.strftime('%-m月') %>のカレンダー</h4>
  <% if @online_rec_channel.calendar_field_memo? %>
    <div class="w-50 mx-auto">
      <p><%= @online_rec_channel.calendar_field_memo %></p>
    </div>
  <% end %>
  <div>
    <%= image_tag @online_rec_channel.image.to_s, width: 700, height: 700, class: "img-fluid mt-5" %>
  </div>

  <h4 class="mt-5"><%= @online_rec_channel.period.strftime('%-m月') %>のZOOMリンク</h4>
  <% if @online_rec_channel.zoom_field_memo? %>
    <div class="w-50 mx-auto">
      <p><%= @online_rec_channel.zoom_field_memo %></p>
    </div>
  <% end %>
  <table class="mt-5 border mx-auto w-50">
    <% current_week_start = @online_rec_channel.online_rec_channel_recreations.first.date.beginning_of_week(:sunday) %>
    <% @online_rec_channel.online_rec_channel_recreations.each do |channel_recs| %>
      <tr>
        <td class="text-start py-2 px-4 border w-50">
          <p class="mb-0"><%= channel_recs.date.strftime("%-d日(#{@wd[channel_recs.date.wday]})") %></p>
          <p class="mb-0 fw-bold"><%= channel_recs.recreation_title %></p>
          <p class="mb-0">
            <%= link_to "レクの詳細・チラシはこちら", sanitize(channel_recs.recreation_link.gsub(/(https?:\/\/[\S]+)/, '<a href="\1" target="_blank">\1</a>')), target: :_blank %>
          </p>
          <% if channel_recs.recreation_memo %>
            <p class="mb-0">備考：<%= channel_recs.recreation_memo %></p>
          <% end %>
        </td>
        <td class="text-start py-2 px-4 border w-50">
          <% if channel_recs.zoom_link? %>
            <%= sanitize(channel_recs.zoom_link.gsub(/(https?:\/\/[\S]+)/, '<a href="\1" target="_blank">\1</a>')) %>
          <% else %>
            <p class="mb-0">zoomリンク公開まで暫くお待ちください</p>
          <% end %>
        </td>
      </tr>
    <% end %>
  </table>
</div>
