<%= render 'shared/customer/header' %>

<main class="customer">
  <div class="container pt-2">
    <nav aria-label="breadcrumb">
      <ol class="breadcrumb">
        <li class="breadcrumb-item">
          <%= link_to 'トップ', root_path %>
        </li>
        <li class="breadcrumb-item">
          <%= link_to 'マイページ', customers_path %>
        </li>
        <li class="breadcrumb-item">
          <%= link_to '依頼履歴', customers_path %>
        </li>
        <li class="breadcrumb-item active">
          <span class="text-secondary">依頼詳細</span>
        </li>
      </ol>
    </nav>
  </div>

  <article class="container py-1 px-0">
    <div class="row">
      <div class="col-md-4">
        <%= link_to customers_path, class: 'clink' do  %>
          <i class="material-icons ">navigate_before</i> 戻る
        <% end %>
      </div>
    </div>
    <div class="row justify-content-center">
      <div class="col-8 mb-5">
        <div class="card order-detail">
          <div class="card-header bg-white">
            依頼内容
          </div>
          <div class="card-body">
            <div class="row py-2">
              <div class="col-lg-3 col-4 label">案件番号</div>
              <div class="col-auto"><%= @order.id %></div>
            </div>
            <div class="row py-2">
              <div class="col-3 label">レク</div>
              <div class="col-9">
                <div class="row align-items-center mt-2">
                  <div class="col-md-4">
                    <picture loading="lazy">
                      <%= image_tag @order.recreation.recreation_images.sliders&.first&.image.to_s, class: 'img-fluid' %>
                    </picture>
                  </div>
                  <div class="col-md-8 recreation">
                    <h1 class="font-16 font-weight-bold line-height-140"><%= @order.recreation_title %></h1>
                    <div class="row category-tags">
                      <%= tag_to_html(@order.recreation, false) %>
                    </div>
                  </div>
                  <div class="col-12 py-1">
                    <%= link_to customers_recreation_path(@order.recreation.id), class: "clink" do %>
                      レクの詳細を見る
                      <i class="material-icons ">navigate_next</i></a>
                    <% end %>
                  </div>
                </div>
              </div>
            </div>
            <% if @order.recreation.flyer.present? %>
              <div class="row py-2">
                <div class="col-md-3"></div>
                <div class="col-md-9 rounded border p-3">
                  <h2 class="font-14 font-weight-bold line-height-120 color-ba10 py-2">チラシ</h2>
                  <span class="font-14 font-weight-normal line-height-120 color-ba08 py-2">レクのチラシをダウンロードして印刷することができます。</span>
                  <div>
                    <% if @order.recreation.flyer.image.identifier&.split('.')&.last&.in?(['pptx', 'ppt']) %>
                      <%= link_to download_pptx_customers_order_path(@order), class: 'clink', download: true do %>
                        チラシをダウンロード
                        <i class="material-icons">navigate_next</i>
                      <% end %>
                    <% else %>
                      <%= link_to @order.recreation.flyer.image.url, class: 'clink', target: '_blank', rel: 'noopener' do %>
                        チラシをダウンロード
                        <i class="material-icons">navigate_next</i>
                      <% end %>
                    <% end %>
                  </div>
                </div>
              </div>
            <% end %>

            <div class="row py-2">
              <div class="col-lg-3 col-4 label">開催日時</div>
              <div class="col-auto">
                <%= dotted_datetime @order.start_at %>
              </div>
            </div>
            <div class="row py-2">
              <div class="col-lg-3 col-4 label">参加人数</div>
              <div class="col-auto"><%= @order.number_of_people %>人</div>
            </div>
            <div class="row py-2">
              <div class="col-lg-3 col-4 label">施設住所</div>
              <div class="col-auto"><%= @order.full_address %></div>
            </div>
            <div class="row py-2">
              <div class="col-lg-3 col-4 label">合計費用(税別)</div>
              <div class="col-auto">
                <%= number_to_currency(@order.total_price_for_customer) %>
              </div>
            </div>

            <div class="row py-2">
              <div class="col-lg-3 col-4 label">ステータス</div>
              <div class="col-auto"><%= I18n.t("customer.order.status.#{@order.status}") %></div>
            </div>

            <div class="row py-2">
              <div class="col-lg-3 col-4 label">関連ファイル</div>
              <div class="col-auto">
                <% @order.recreation.recreation_images.materials.each do |file| %>
                  <div>
                    <%= link_to file.image.file.filename, file.image.url, class: 'clink mr-5', target: '_blank', rel: 'noopener' %>
                  </div>
                <% end%>
              </div>
            </div>

            <% if @order.recreation_kind.online? %>
              <div class="row py-2">
                <div class="col-lg-3 col-4 label">Zoom情報</div>
                <div class="col-auto">
                  <% if @order.zoom_url %>
                    <%= Rinku.auto_link(simple_format(h(@order.zoom_url)), :all, 'target="_blank"').html_safe %>
                  <% else %>
                      開催日までに発行されますので、今しばらくお待ちください
                  <% end %>
                </div>
              </div>
            <% end %>

            <div class="row p-3 justify-content-around">
              <div class="col action pb-3">
                <div class="action-header pt-3">
                  チャット
                </div>
                <div class="py-3">終了報告が完了するまでパートナーとチャットができます。</div>
                <% if @order.report_status&.accepted? %>
                    <span class="btn btn-cprimary font-14 py-2 px-4 font-weight-bold text-white disabled ">チャットへ進む</span>
                <% else %>
                  <%= link_to 'チャットへ進む', chat_customers_order_path(@order.id), class: 'btn btn-cprimary  font-14 py-2 px-4 font-weight-bold text-white' %>
                <% end %>
              </div>
              <% if @order&.report&.id.present? %>
                <div class="col action pb-3">
                  <div class="action-header pt-3">終了報告</div>
                  <div class="py-3" style="min-height: 5rem;">レクが開催後、パートナーから終了報告の承認依頼が届きます。内容を確認して承認してください。
                  </div>
                  <% if @order.report_status.accepted? %>
                    <span class="btn btn-cprimary font-14 py-2 px-4 font-weight-bold text-white disabled ">終了報告を確認済み</span>
                  <% else %>
                    <%= link_to '終了報告を確認', edit_customers_report_path(@order.report&.id), class: 'btn btn-cprimary font-14 py-2 px-4 font-weight-bold text-white' %>
                  <% end %>
                </div>
              <% end %>
            </div>

            <!-- 小計 -->
            <div class="card mt-4">
              <div class="card-header bg-white pb-3">
                レク費用
              </div>
              <div class="card-body py-0">
                <div class="row justify-content-between border-bottom-dotted py-2">
                  <div class="col-auto">開催費</div>
                  <div class="col-auto">
                    <%= number_to_currency(@order.price) %>
                  </div>
                </div>
                <div class="row justify-content-between border-bottom-dotted py-2">
                  <div class="col-auto">材料費/1人<br><span
                    class="text-muted font-12 color-ba08">※材料費は人数分の費用が発生します</span></div>
                  <div class="col-auto">
                    <%= number_to_currency(@order.material_price) %>
                  </div>
                </div>
                <div class="row justify-content-between border-bottom-dotted py-2">
                  <div class="col-auto">交通費</div>
                  <div class="col-auto">
                    <%= number_to_currency(@order.transportation_expenses) %>
                  </div>
                </div>
                <div class="row justify-content-between border-bottom-dotted py-2">
                  <div class="col-auto">諸経費</div>
                  <div class="col-auto">
                    <%= number_to_currency(@order.expenses) %>
                  </div>
                </div>
                <div class="row justify-content-between border-bottom-dotted py-2">
                  <div class="col-auto">サポート費用</div>
                  <div class="col-auto">
                    <%= number_to_currency(@order.support_price) %>
                  </div>
                </div>
                <% if @order.recreation_kind.online? %>
                  <div class="row justify-content-between border-top-dotted py-2">
                    <div class="col-auto align-self-center">
                      <span id="facilityCount">追加施設費 / <%= @order.number_of_facilities %> 施設</span>
                    </div>
                    <div class="col-auto" id="additionalPrice">
                      <% if @order.number_of_facilities.present? %>
                        <%= number_to_currency(@order.number_of_facilities * @order.additional_facility_fee) %>
                      <% else %>
                        <%= number_to_currency(0) %>
                      <% end %>
                    </div>
                    <div class="col-12">
                      <span class="text-muted font-12 color-ba08">
                        ※1施設追加する事に<%= number_to_currency @order.additional_facility_fee %>が発生します
                      </span>
                    </div>
                  </div>
                <% end %>

                <div class="row justify-content-between border-top py-3">
                  <div class="col-auto">合計(税別)</div>
                  <div class="col-auto">
                    <%= number_to_currency(@order.total_price_for_customer) %>
                  </div>
                </div>

                <% if @order.coupon_code? && @order.coupon_code == "えぶりぷらす" %>
                  <div class="row justify-content-between border-top py-3">
                    <div class='alert alert-warning' role='alert'>
                      <div class='font-weight-boild'>クーポン適用中のため合計金額は「&yen; 0」となります</div>
                      <div>合計金額が表示されていても請求は&yen; 0となります。ご安心ください。</div>
                    </div>
                  </div>
                <% end %>
              </div>
            </div>

            <div class="card mt-4">
              <div class="card-body">
                <h5 class="card-title title-b">注意事項</h5>
                <p>・一度承認されたレクのキャンセルはできません。規約に準じた日程の変更は可能です。詳しくは利用規約をご確認ください。</p>
                <p>・材料費、オンラインレクの追加施設数、諸経費は開催までの間にパートナーとチャットで相談をして変更することが可能です。その場合、金額の変動がございます。</p>
                <p>・追加施設がある場合、追加事業所にはzoomURLの共有をお願いします。</p>
                <p>・正式依頼後に、パートナーからチャットで共有をされます。コピーをしてzoomにアクセスをお願いします。</p>
                <p>・材料費が必要なレクは参加人数が増える場合や減る場合はパートナーとチャット相談をしてくださ　い。人数が減る場合、レクによっては準備した分の材料費分の請
                  求が発生する場合がございます。詳しくはパートナーにご確認をお願い致します。</p>
                <p>・ご不明な点がありましたらよくある質問またはお問い合わせをお願いします。</p>
              </div>
            </div>

          </div>
        </div>
      </div>
    </div>
  </article>
</main>
