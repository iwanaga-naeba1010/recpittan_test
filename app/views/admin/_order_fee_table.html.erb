<% if kind == :customer %>
  <table>
    <tr>
      <td>開催費</td>
      <td><%= number_to_currency order.regular_price %></td>
    </tr>

    <tr>
      <td>材料費(合計)</td>
      <td><%= number_to_currency order.total_material_price_for_customer %></td>
    </tr>

    <tr>
      <td>交通費</td>
      <td><%= number_to_currency order.transportation_expenses %></td>
    </tr>

    <tr>
      <td>諸経費</td>
      <td><%= number_to_currency order.expenses %></td>
    </tr>

    <tr>
      <td>サポート費用</td>
      <td><%= number_to_currency order.support_price %></td>
    </tr>

    <tr>
      <td>施設追加費用</td>
      <td><%= number_to_currency order.total_facility_price_for_customer %></td>
    </tr>

    <tr>
      <td>合計</td>
      <td><%= number_to_currency order.total_price_for_customer %></td>
    </tr>
  </table>
<% end %>

<% if kind == :partner %>
  <table>
    <tr>
      <td>謝礼</td>
      <td><%= number_to_currency order.instructor_amount %></td>
    </tr>

    <tr>
      <td>材料費(合計)</td>
      <td><%= number_to_currency order.total_material_price_for_partner %></td>
    </tr>

    <tr>
      <td>交通費</td>
      <td><%= number_to_currency order.transportation_expenses %></td>
    </tr>

    <tr>
      <td>諸経費</td>
      <td><%= number_to_currency order.expenses %></td>
    </tr>

    <tr>
      <td>施設追加費用</td>
      <td><%= number_to_currency order.total_facility_price_for_partner %></td>
    </tr>

    <tr>
      <td>合計</td>
      <td><%= number_to_currency order.total_price_for_partner %></td>
    </tr>
  </table>
<% end %>
