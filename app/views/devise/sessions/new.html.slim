main.auth
  .container.pt-2
    nav[aria-label='breadcrumb']
      ol.breadcrumb
        li.breadcrumb-item
          = link_to 'トップ', root_path
        li.breadcrumb-item.active[aria-current='page']
          span ログイン

  .container
    .row.justify-content-center.my-5
      .col-md-8
        .card.p-4
          .card-body
            .text-center.mb-5
              = image_pack_tag 'logo.png', class: 'logo-navbar'
            p.text-muted ユーザ名とパスワードを入力して下さい。

            = simple_form_for(resource, as: resource_name, url: session_path(resource_name)) do |f|
              .input-group.mb-3
                .input-group-prepend
                  span.input-group-text
                    span.material-icons email
                .form-group.flex-grow-1
                  = f.input :email, label: false, required: true, input_html: { class: 'form-control' }

              .input-group.mb-3
                .input-group-prepend
                  span.input-group-text
                    span.material-icons lock
                .form-group.flex-grow-1
                  = f.input :password, label: false, required: true, hint: ("#{@minimum_password_length} 文字以上で入力してください" if @minimum_password_length), placeholder: 'パスワード', input_html: { autocomplete: 'new-password' }

              .row.justify-content-between
                .col-auto.text-center
                  span.forgot-password
                    | パスワードを忘れた方は
                    = link_to 'こちら', new_user_password_path
                .col-md-4.text-center
                  = f.button :submit, 'ログイン', class: 'btn btn-cprimary rounded-pill w-100 font-14 py-3 px-4 font-weight-bold text-white'

          .card-body.border-top.mt-4
            .row.justify-content-center
              .col-md-4.text-center
                = link_to '新規登録はこちら', new_user_registration_path, class: 'registration-link'
