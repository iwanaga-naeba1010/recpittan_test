<div class="header">
  <h2 class="text-white font-weight-bold p-2">
    <% if params[:is_accepted] == 'true' %>
      開催決定済の案件
    <% else %>
      開催のご相談
    <% end %>
  </h2>
  <%= render 'shared/partner/sidebar_toggle' %>
</div>

<div class="my-3 mx-2">
  <% if params[:is_accepted] == 'true' %>
    正式依頼が承認され開催が決定した案件の一覧です。
  <% else %>
    正式依頼確認中の案件は、早めのご回答をお願いいたします。
  <% end %>
</div>

<div class="my-3 mx-2">
  <% if params[:is_accepted] == 'true' %>
    <%= link_to '開催日順(降順)', partners_path(column: :start_at, order: :desc, is_accepted: @is_accepted), class: ' text-white btn btn-primary  p-3' %>
    <%= link_to '開催待ち → 終了報告承認済み', partners_path(column: :status, order: :asc, is_accepted: @is_accepted), class: ' text-white btn btn-primary  p-3' %>
  <% else %>
    <%= link_to '依頼番号順', partners_path(column: :id, order: :desc, is_accepted: @is_accepted, is_public: @is_public), class: ' text-white btn btn-primary  p-3' %>
    <%= link_to '正式依頼確認中 → 相談中',
        partners_path(column: :status, order: :desc, is_accepted: @is_accepted, is_public: @is_public), class: ' text-white btn btn-primary  p-3' %>
  <% end %>
</div>

<% @orders.each do |order| %>
  <%= link_to chat_partners_order_path(order.id) do %>
    <div class="order-item my-2 border-bottom p-3">
      <div class="">
        <h3>
          <%= order.user.company.facility_name.truncate(20) %>
        </h3>
      </div>
      <div class="mb-2">
        <%= order.recreation_title %>
      </div>
      <div class="mb-2">
        開催形態:
        <%= order.recreation.kind_text %>
      </div>
      <div class="mb-2">
        開催希望日:
        <%= dotted_datetime order.start_at %>
      </div>
      <small class="mb-2 mr-3">
        依頼番号:
        <%= order.id %>
      </small>
      <small class="mb-2 mr-3">
        更新日:
        <%= dotted_datetime order.updated_at %>
      </small>
      <div class="status mb-2">
        <span class="status__cunsult p-1">
          <%= I18n.t("partner.order.status.#{order.status}") %>
        </span>
      </div>
    </div>
  <% end %>
<% end %>
